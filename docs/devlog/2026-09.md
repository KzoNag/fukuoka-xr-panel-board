# devlog 2026-09

## 2026-09-18
- やったこと: 明日(09-19)のパネルディスカッション用進行ボードを一晩で作成・検証。参加者(匿名スマホ)の投稿・▲投票、
  進行役が自由に選んだトピックをボードへ7分タイマー付き表示、`start.sh` で cloudflared 公開+URLをQRに自動反映。
  ローカル・トンネル経由の両方で投稿/投票/タイマー/ロングポーリング更新、サーバー落ち→自動再起動、トンネル落ち→張り直しを実測。
- わかったこと:
  - Claude Artifact の共有DBは組織内限定で匿名参加者に使えない(ADR 0001)
  - cloudflared クイックトンネルは SSE をバッファして本文が届かない → ロングポーリングに変更(ADR 0002)
  - 張り直した新トンネルURLを自分のMacから即叩くと NXDOMAIN が負キャッシュされて自分だけ繋がらなくなる。参加者側は問題なし。
    `start.sh` は 1.1.1.1 にDNS掲載を確認してからQRに出すようにした
  - `set -e` 下で `start_tunnel` を裸で呼ぶとトンネル失敗でサーバーごと死ぬ(critic指摘) → `set -e` を外して再試行型に書き直し
- 追記: ユーザー要望でボードと進行役画面を1画面にマージ(`/board?key=` で操作ボタンが出る。進行役の画面をそのまま画面共有するため)。admin.html は削除、`/admin` は互換で同じ画面。
- 次にやること: 当日 `./start.sh --fresh` で起動 → 表示された `/board?key=…` をフルスクリーンで画面共有 → オープニングでQR告知。
  イベント後、使い勝手のフィードバックを devlog に残す(タイマー7分は妥当だったか、投票は機能したか)。

## 2026-09-22
- やったこと: public 公開前のセキュリティチェック(秘密情報・個人情報の混入なし、`data/` は gitignore 済み)。
  リポジトリを MIT にし、同梱の qrcode-generator(MIT)の表記を THIRD_PARTY_NOTICES.md に追加。git 管理を始めて GitHub に public で公開。
- わかったこと: 当日の実績は運営トピック6本・参加者投稿0件・8人で15票・2トピック消化。投稿は出ないが投票はされる。
- 次にやること: 再利用するなら先にセキュリティチェックで挙げた弱点(URLに残る進行役キー、他人の票を取り消せる、0.0.0.0 待ち受け等)を直す。
  置き場所が `~/tmp` のままなので、残すなら別の場所へ移す。
- 追記: README 冒頭に「メンテナンスしていません」、末尾に「再利用する前に」(既知の弱点5項目と直し方)を追加。
  issue にはしない(保守しないリポジトリに open の issue を並べないため。再利用時に切り出す)。
